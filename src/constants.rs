pub const NUM_VERTICES: u32 = 1000u32;

pub const NUM_TRIANGLES: u32 = 3000u32;

pub const BITS_PER_PASS: u32 = 8;

pub const HISTOGRAM_SIZE: u32 = 1 << BITS_PER_PASS;

pub const WORKGROUP_SIZE: u32 = 256u32;

pub const ITEMS_PROCESSED_PER_LANE_HISTOGRAM_PASS: u32 = 1u32;

pub const ITEMS_PER_HISTOGRAM_PASS: u32 = WORKGROUP_SIZE * ITEMS_PROCESSED_PER_LANE_HISTOGRAM_PASS;

pub const ITEMS_PER_LARGE_PREFIX: u32 = 256u32;

pub const MAX_ITEMS_IN_SMALL_PREFIX: u32 = 1024u32;

pub const NUM_PASSES: u32 = 8;

pub const RNG_SEED: u64 = 7;
